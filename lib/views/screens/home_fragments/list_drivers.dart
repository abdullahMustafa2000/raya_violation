import 'package:flutter/material.dart';

class DriversListFragment extends StatelessWidget {
  const DriversListFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
